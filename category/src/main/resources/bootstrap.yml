spring:
  application:
    name: category-service
  cloud:
    config:
      uri: http://localhost:8888
      # Exception is thrown during start up if config server cannot be connected
#      fail-fast: true
      username: user
      password: password